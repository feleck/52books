%h3= @author.full_name
%p 
  = link_to 'Back', authors_path, class: 'btn'
  = link_to 'Edit', edit_author_path(@author), class: 'btn'
  = link_to 'Delete', author_path(@author), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-dnger'
